{
  "name": "customer-work-crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "dev": "pnpm --filter @crm/web dev",
    "build": "pnpm --filter @crm/web build",
    "typecheck": "pnpm --filter @crm/web typecheck",
    "test": "node --test packages/domain/*.test.mjs scripts/release-check.test.mjs scripts/check-production-approval.test.mjs scripts/check-staging-release.test.mjs && pnpm --filter @crm/api test",
    "live:setup": "pnpm --filter @crm/api run setup",
    "live": "pnpm --filter @crm/api start",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "test:postgres": "pnpm --filter @crm/api exec node --test test-postgres/*.test.mjs"
  },
  "engines": { "node": ">=24 <25" }
}
